---
name: opencrow-web-toolbox
description: Placeholder for future OpenCROW web CTF tooling. Use only when the task clearly belongs to web exploitation and no narrower installed OpenCROW toolbox applies yet.
---

# OpenCROW Web Toolbox

This toolbox is intentionally a placeholder. There are no dedicated web CTF tools mapped into OpenCROW yet, so the skill only exists to reserve the category and make the gap explicit.

## Current State

- Use this skill only to acknowledge that the repo has a web category but not a populated tool stack yet.
- Run the dummy tool if you need an explicit reminder in terminal output:

```bash
python ~/.codex/skills/opencrow-web-toolbox/scripts/todo_web_tool.py
```

## TODO

- TODO: update with real tools.
- Candidate future contents include HTTP clients, web fuzzers, intercepting proxies, and browser-oriented exploit helpers if they become part of the installed stack.

## Resources

- `scripts/todo_web_tool.py`: dummy placeholder command for the empty toolbox.
- `references/tooling.md`: notes the current gap and future direction.
